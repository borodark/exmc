# Hide GPU during tests. EXLA tries to init CUDA on machines with NVIDIA
# drivers, and if the GPU is busy or has < 1GB free, the EXLA.Client
# GenServer crashes and cascades to all JIT-using tests.
#
# To test with GPU: CUDA_VISIBLE_DEVICES=0 mix test
CUDA_VISIBLE_DEVICES=
